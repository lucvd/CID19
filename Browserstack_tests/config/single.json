{
  "server": "hub.browserstack.com",

  "capabilities": {
    "build": "behave-browserstack",
    "name": "single_test",
    "browserstack.debug": true
  },

  "environments": [{
    "browser": "chrome"
  }]
}